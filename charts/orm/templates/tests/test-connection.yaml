apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orm.fullname" . }}-test-connection"
  labels:
    {{- include "orm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
