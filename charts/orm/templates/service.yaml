apiVersion: v1
kind: Service
metadata:
  name: {{ include "orm.fullname" . }}
  labels:
    {{- include "orm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "orm.selectorLabels" . | nindent 4 }}
