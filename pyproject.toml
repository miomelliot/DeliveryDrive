[project]
name = "dd"
version = "0.1.0"
requires-python = ">=3.13"
description = "Add your description here"

dependencies = ["grpcio>=1.63.0", "protobuf>=4.23.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["dd"]

[dependency-groups]
dev = ["grpcio-tools>=1.71.0", "nox[uv]>=2025.5.1"]
